# Project-specific zzcollab configuration
# Overrides user config (~/.zzcollab/config.yaml)
# Commit this file to version control.
docker:
  default_profile: publishing
defaults:
  profile_name: publishing
